import { useEffect, useMemo, useState } from 'react';
import { ArrowLeft, ArrowRight, Heart, Minus, Plus, ShieldCheck, ShoppingBag, Star, Truck } from 'lucide-react';
import ReviewSection from './ReviewSection';
import './ProductDetail.css';

type Product={id:number;name:string;slug:string;description:string;price:number;rating:number;category:string;imageUrl:string;variantId?:number;size?:string;color?:string};
type Variant={id:number;sku:string;size:string;color:string;stockQuantity:number;price:number};
type Detail={id:number;name:string;slug:string;description:string;price:number;rating:number;category:string;categorySlug:string;imageUrl:string;variants:Variant[]};
const money=(n:number)=>`₹${n.toLocaleString('en-IN')}`;
const api=import.meta.env.VITE_API_URL||'http://localhost:8080';
export default function ProductDetail({productId,onBack,onAdd,onWishlist,liked}:{productId:number;onBack:()=>void;onAdd:(product:Product)=>void;onWishlist:(id:number)=>void;liked:boolean}){
 const [detail,setDetail]=useState<Detail|null>(null),[loading,setLoading]=useState(true),[size,setSize]=useState(''),[color,setColor]=useState(''),[quantity,setQuantity]=useState(1),[error,setError]=useState('');
 useEffect(()=>{setLoading(true);setError('');fetch(`${api}/api/v1/products/${productId}/detail`).then(r=>r.ok?r.json():Promise.reject()).then((d:Detail)=>{setDetail(d);const first=d.variants.find(v=>v.stockQuantity>0);if(first){setSize(first.size);setColor(first.color)}}).catch(()=>setError('We could not load this product right now.')).finally(()=>setLoading(false))},[productId]);
 const sizes=useMemo(()=>Array.from(new Set((detail?.variants||[]).map(v=>v.size))),[detail]);
 const colors=useMemo(()=>Array.from(new Set((detail?.variants||[]).map(v=>v.color))),[detail]);
 const selected=detail?.variants.find(v=>v.size===size&&v.color===color);
 if(loading)return <section className="detailPage"><div className="detailLoading"><span className="micro">GENZ STORE</span><h1>Loading product…</h1></div></section>;
 if(error||!detail)return <section className="detailPage"><button className="detailBack" onClick={onBack}><ArrowLeft/> Back to shop</button><div className="detailLoading"><h1>Product unavailable</h1><p>{error||'This product could not be found.'}</p></div></section>;
 const add=()=>{if(!selected||selected.stockQuantity<1){setError('Select an available size and color.');return}if(quantity>selected.stockQuantity){setError(`Only ${selected.stockQuantity} left in stock.`);return}onAdd({...detail,variantId:selected.id,size:selected.size,color:selected.color,price:selected.price,quantity});setError('Added to your bag.');};
 return <section className="detailPage"><div className="detailTop"><button className="detailBack" onClick={onBack}><ArrowLeft/> Back to shop</button><div className="detailBrand">GenZ<span>Store</span><small>WEAR YOUR STORY</small></div><div className="detailTopRight"><span>GENZ STORE</span><ShoppingBag/></div></div><div className="detailBreadcrumb"><span>SHOP</span><b>/</b><span>{detail.category.toUpperCase()}</span><b>/</b><strong>{detail.name.toUpperCase()}</strong></div><div className="detailGrid"><div className="detailVisual"><img src={detail.imageUrl} alt={detail.name}/><span>GENZ STORE / EDIT</span></div><div className="detailInfo"><div className="detailMeta"><span>{detail.category}</span><button onClick={()=>onWishlist(detail.id)} aria-label="Wishlist"><Heart fill={liked?'currentColor':'none'}/></button></div><h1>{detail.name}</h1><div className="detailRating"><Star size={13} fill="currentColor"/> {detail.rating} <span>·</span> Loved by the GenZ community</div><div className="detailPrice">{money(selected?.price??detail.price)}</div><p className="detailDescription">{detail.description}</p><div className="detailRule"/><div className="optionBlock"><div className="optionHead"><strong>Size</strong><span>{size||'Select a size'}</span></div><div className="optionRow">{sizes.map(s=>{const available=detail.variants.some(v=>v.size===s&&v.stockQuantity>0);return <button key={s} disabled={!available} className={size===s?'active':''} onClick={()=>{setSize(s);const next=detail.variants.find(v=>v.size===s&&v.stockQuantity>0);if(next)setColor(next.color)}}>{s}</button>})}</div></div><div className="optionBlock"><div className="optionHead"><strong>Color</strong><span>{color||'Select a color'}</span></div><div className="colorRow">{colors.map(c=>{const available=detail.variants.some(v=>v.color===c&&v.stockQuantity>0);return <button key={c} disabled={!available} className={color===c?'active':''} onClick={()=>{setColor(c);const next=detail.variants.find(v=>v.color===c&&v.stockQuantity>0);if(next)setSize(next.size)}}><i/>{c}</button>})}</div></div><div className="detailPurchase"><div className="detailQty"><button onClick={()=>setQuantity(q=>Math.max(1,q-1))}><Minus/></button><span>{quantity}</span><button onClick={()=>setQuantity(q=>Math.min(selected?.stockQuantity||1,q+1))}><Plus/></button></div><button className="detailAdd" onClick={add}><ShoppingBag/> Add to bag <ArrowRight/></button></div><div className="detailStatus">{selected?<><span className={selected.stockQuantity<=5?'lowStock':''}>{selected.stockQuantity<=5?`Only ${selected.stockQuantity} left`:'In stock'}</span><small>SKU {selected.sku}</small></>:<span>Select options to check availability</span>}</div>{error&&<div className={`detailNotice ${error.startsWith('Added')?'success':''}`}>{error}</div>}<div className="detailPromises"><div><Truck/><span><strong>Free shipping</strong>On orders above ₹999</span></div><div><ShieldCheck/><span><strong>Secure checkout</strong>Protected payments</span></div></div></div></div><div className="detailStory"><span className="micro">THE GENZ EDIT</span><h2>Designed for everyday rotation.</h2><p>Build your fit around the piece, then let AI Stylist help you finish the look.</p></div><ReviewSection productId={detail.id}/></section>;
}
